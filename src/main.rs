fn main() {
    println!("Hello, world!");

    // unsafe {
    //     let align = std::mem::align_of::<u8>();
    //     let sz = 10 * std::mem::size_of::<u8>();
    //     let p = std::alloc::alloc(std::alloc::Layout::from_size_align(sz, align).unwrap());
    // }
}
